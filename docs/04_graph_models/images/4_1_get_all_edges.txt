%%{init: {"theme":"base"}}%%
sequenceDiagram
    actor User
    participant GraphModel
    participant ConnectionInterface
    participant Queries Module
    participant Database
    note right of User: Expand knowledge graph
    User ->>+ GraphModel: get_all_edges(cim_class)
    %% alt cim_class not in graph
    %%     GraphModel -->>- User: None
    %% else 
    %%     GraphModel ->> GraphModel: exit
    %%     %% GraphModel -->>- User: None
    %% end
    GraphModel ->>+ ConnectionInterface: get_all_edges(graph, cim_class)
    ConnectionInterface ->>+ Queries Module: get_all_edges_query()
    Queries Module -->>- ConnectionInterface: str[query text]
    ConnectionInterface ->>+ Database: execute(query text)
    Database -->>- ConnectionInterface: dict[query response]
    loop
        ConnectionInterface ->> ConnectionInterface: populate attributes
        ConnectionInterface ->> ConnectionInterface: create new objects
    end
    ConnectionInterface -->>- GraphModel: None
    GraphModel -->>- User: None